version: 2.1

orbs:
    lambda-dev: lambda-dev/lambda-tunnel@0.0.2

workflows:      
    basic_workflow:
        jobs:
          - lambda-dev/with_tunnel:
              steps:
                - run: 
                    command: |
                      npm install
                      node_modules/.bin/nightwatch -e chrome